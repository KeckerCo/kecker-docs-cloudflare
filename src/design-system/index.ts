/**
 * Kecker Design System
 *
 * Re-exports all design tokens. Import from here in Storybook and scripts:
 *
 *   import { color, font, fontSize, space } from '../design-system';
 *
 * For CSS usage, see src/styles/global.css (:root block).
 * CSS custom properties mirror these values and must be kept in sync.
 */

export * from './tokens';
