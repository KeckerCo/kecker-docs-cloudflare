{
  "name": "kecker-docs-cloudflare",
  "version": "1.0.0",
  "description": "Kecker docs - Cloudflare Pages static site",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --dev && npx serve dist -l 5174"
  },
  "dependencies": {
    "marked": "^17.0.5"
  }
}
